class Solution {

    TreeNode prev = null;

    public void flatten(TreeNode root) {

        if (root == null) {
            return;
        }

        // Reverse preorder: right -> left
        flatten(root.right);
        flatten(root.left);

        root.right = prev;
        root.left = null;

        prev = root;
    }
}
